generated: '2026-09-11'
method: searched
source: |-
  NuGet (azuresearch-usnc.nuget.org + registration metadata) and npm
  (registry.npmjs.org); both packages are published by Routebase GmbH.
packages:
- name: Routebase.Cli
  registry: nuget
  url: https://www.nuget.org/packages/Routebase.Cli
  official: true
  language: dotnet
  version: 2.0.0
  published: '2026-09-01'
  install: dotnet tool install --global Routebase.Cli
  note: |-
    First-party .NET global tool (author "Routebase GmbH"). Drives the CI, spec,
    security and docs-as-code endpoint groups and gates CI runs. v2.0.0 (2026-09-01)
    changed exit-code mapping (exit 4 for auth errors) per changelog 1.8.0. Earlier
    releases 1.0.0 and 1.0.1 both 2026-08-21.
- name: routebase-mcp
  registry: npm
  url: https://www.npmjs.com/package/routebase-mcp
  official: true
  language: javascript
  version: 1.1.3
  published: '2026-09-08'
  install: npx -y routebase-mcp@latest --stdio
  note: |-
    First-party stdio bridge to the Routebase MCP server ("Routebase MCP server CLI"
    by Routebase GmbH). Connects agents (Claude Code, Cursor, VS Code) to
    mcp.routebase.dev over stdio using the ROUTEBASE_API_KEY env var. See mcp/.
